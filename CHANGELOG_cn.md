# 更新日志 (Changelog)

所有关于 得到大脑（原Get笔记）导入器 (GetImporter) 插件的显著变更都将记录在此文件中。

该格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，
且本项目遵循 [语义化版本规范 (Semantic Versioning)](https://semver.org/spec/v2.0.0.html)。

## [3.9.0] - 2026-08-09

### 新增 (Added)
- 🚀 **Web 私有 API 全通道同步**：全新推出 Web 私有 API 同步渠道，面向非 PRO 用户提供完美支持！
  - **全量/增量下拉**：支持拉取所有个人笔记，支持自动增量更新，可选直接调用网页端 Markdown 导出接口以保留高保真原文。
  - **极速单篇推送**：在编辑器中点击即刻将当前修改后的笔记推送到云端（支持将包含的本地图片自动上传并替换为云端链接）。
  - **Markdown 渲染器**：内置自定义 Markdown 转 Web JSON 渲染器，支持将 Obsidian 特有语法（如高亮、任务列表、数学公式）精准还原到云端。
- 🎨 **设置面板标签页重构**：将冗长的单页设置重构为现代化的标签页结构（Tab Navigation），分为“同步与拉取”、“双向与推送”、“高级选项”和“🔑 授权”四大板块，逻辑更清晰。
- 🧩 **编辑器底层扩展 (CodeMirror)**：引入 `@codemirror/view` 与 `@codemirror/state`，新增 `aggregate-marker-hider` 扩展，现在可实现对双向同步的内部标记（如 `<!-- getnote:content:start -->`）在阅读器和实时预览模式下的无感隐藏。
- 🌐 **全量国际化支持 (i18n)**：重构并上线了完整的双语字典架构，插件现已实现 100% 纯英文环境适配。自动根据系统/Obsidian配置无缝切换所有设置、模态框、命令和系统提示。

### 更改 (Changed)
- 🔄 **底层核心解耦**：深度重构并分离了 Web 渠道与 OpenAPI 渠道的同步核心引擎（如抽象 `BinaryDownloader` 等），使其各自迭代互不干扰，显著提升同步健壮性。
- ⚙️ **Web 登录交互**：将 Web 渠道的“测试连接”与“退出登录”等散落操作收拢，测试连接新增明确的 Notice 反馈。

### 修复 (Fixed)
- 🐛 **退出登录失效Bug**：修复了“退出登录”后底层 `<webview>` 仍残留 Cookie 与 Session，导致重新打开登录窗口时自动登回旧账号的严重问题。现在退出后会自动执行全面清理。
- 🐛 **API 容错与防污染**：修复 Web 私有 API 增量写入时携带 `uid` 字段导致服务端误判的 bug（现在下拉屏蔽 `uid` 写入）；并增强了针对 Token 过期的网络错误识别分类能力。

---

## [3.8.1] - 2026-08-08

### 修复 (Fixed)
- 🐛 **增量同步全量读取配额问题**：修复了"增量同步"模式下列表分页每次都从 `since_id=0` 开始全量遍历的 bug。现在增量模式会自动提取本地所有笔记 YAML 前方数据中的最大 `uid` 作为 `since_id` 游标（完全无状态、零额外配置），下次同步仅拉取游标之后的新笔记，**2000 条笔记场景下月配额消耗从 30,000+ 次降至 < 300 次**

---

## [3.8.0] - 2026-05-31

### 更改 (Changed)
- 🔄 **双向同步边界收敛**：根据 OpenAPI 实际可写模型重新明确双向同步范围，上行只写回 Get 笔记支持的 `title`、`content`、`tags`。
- 📝 **可写正文区机制**：OpenAPI 下行笔记以 `<!-- getnote:content:start -->` 到 `<!-- getnote:content:end -->` 作为唯一可上行正文区域；转写、附件、来源、引用、父子关系展示等内容保留为本地只读展示。
- 🧮 **Hash 基线对齐**：本地变更检测统一基于可写正文区和 tags，避免转写、附件等渲染态内容导致误判为本地修改。

### 修复 (Fixed)
- 🛡️ **上行污染防护**：修复整篇生成 Markdown 被写回 Get `content` 的风险，避免转写文本、附件区、来源摘要等派生内容污染云端正文。
- 🚫 **统一上传跳过规则**：手动上传、定时上传、批量扫描和保存触发的单文件上传现在共用同一套跳过规则，覆盖按日期合并文件、冲突文件和冲突副本。
- 📅 **按日期合并安全边界**：带 `uids` 的合并文件被视为只读下行文件并跳过上行；重复 `uid` 段落保持 skipped 状态，不再被误计为 updated。
- ⚔️ **冲突文件安全处理**：跳过 `.conflict-*` 文件、`conflict_type: bidirectional_sync` 文件以及带 `remote_uid` 的冲突副本，避免被创建成新的云端笔记。
- ⚙️ **不安全配置组合拦截**：设置页禁止 `mergeByDate` 与保存时双向上行同时启用，和“合并文件不上行”的实现边界保持一致。

---

## [3.7.0] - 2026-05-22

### 新增 (Added)
- 📊 **全局配额管理器**：统一管理 OpenAPI 的 read / write / write_note 三类配额，所有模块共享配额状态，避免重复查询和状态不一致
- 📈 **配额可视化面板**：主界面和侧边栏实时显示剩余配额（今日/月度），低于阈值时自动警告，配额耗尽时显示重置倒计时
- 🔔 **配额熔断机制**：收到 API 配额耗尽错误（10203）时自动停用相关功能，避免在最坏时机被强制中断
- 🔍 **右键快捷语义检索**：支持在任意笔记编辑区选中段落并右键，通过上下文菜单一键 `以选中文本进行语义检索`，自动展开右侧检索视图召回云端知识
- 🔗 **全通道双链转换支持**：双链还原功能全量覆盖至 **ZIP 本地导入** 与 **OpenAPI 云端同步** 双通道，完美还原正文、引用及网页摘要中所有被转义的 `\[\[链接\]\]`

### 修复 (Fixed)
- 🐛 **空文件夹创建问题**：修复了在某些情况下创建空文件夹的问题，优化了目录结构管理
- 🐛 **OpenAPI 调用次数统计**：修复了配额消耗追踪不准确的问题，现在每次 API 调用都会正确记录并更新全局配额状态
- 📅 **按日期合并机制最佳实践重构**：
  - **YAML 格式净化**：自动剥离追加子笔记的重复 Frontmatter（YAML 块），保证合并后文件在头部有且仅有一个合法 YAML 块
  - **元数据 uids 数组聚合**：自适应将单值 `uid` 智能升级为 `uids: ["uid1", "uid2", ...]` 数组，并动态更新最后修改时间（modified）
  - **本地 UidIndex 兼容性提升**：支持解析 `uids` 数组建立反向映射，二次增量预检能 100% 匹配到合并文件内的所有笔记，**彻底解决因无法检索 UID 导致的多余 API 同步请求与配额流失**
- 🐛 **ZIP 导入多双链首处替换 Bug 修复**：将 ZIP 通道下的 `.replace` 重构为全局 `.replaceAll`，解决了一篇卡片内只能转换第一个双链的 Bug
- 📱 **Obsidian 移动端加载彻底修复与极致构建调优**：
  - **顶级依赖完全消除**：重构了 `const.ts`、`importer.ts`、`auth.ts`、`exporter.ts`、`main_ui.ts`，将静态的 `path`, `os`, `fs-extra`, `playwright`, `decompress` 顶级静态导入全部改造为**平台守护下的惰性（Lazy）加载函数**；
  - **极致 external 构建调优**：在 `esbuild.config.mjs` 中将 Node.js 及物理大包全部标记为 external，防止打包揉合。**构建包体积由 4.0MB 骤降至 963.9KB（缩小了整整 4 倍！）**，编译打包速度缩短至 **42ms**；
  - **跨端平滑降级**：彻底解决了移动端（iOS/Android/iPadOS）因顶级 `require("fs")` 被 Obsidian 安全拦截导致的插件加载失败（Plugin failure）。移动端秒级启用，OpenAPI 和 RAG 全量使用；电脑桌面端动态 require 载入，本地 ZIP 导入和 Playwright 依然完美无损！

---

## [3.6.0] - 2026-05-21

### 更改 (Changed)
- 🎨 **"动静分离"架构重构**：将持久配置（API 密钥、偏好设置、文件夹映射）迁移到系统原生的插件设置选项卡中，弹窗 Modal 仅作为交互式任务执行器
- ⚙️ **设置面板优化**：配置项按功能分组（基础设置、OpenAPI 配置、双向同步、可视化选项、高级选项），提升用户体验和可维护性
- 🔧 **UI 交互改进**：主面板专注于同步操作和状态展示，所有配置管理统一在设置选项卡中完成

---

## [3.5.0] - 2026-05-20

### 新增 (Added)
- 🔄 **OpenAPI 双向同步**：支持将 Obsidian 中创建/修改/删除的笔记推送到 Get 笔记云端，实现真正的双向同步
- 🖼️ **图片笔记上传**：自动识别 Markdown 中的本地图片，通过 OSS 上传后创建 img_text 类型笔记，支持全局 SHA-256 去重避免重复上传
- 🗑️ **远端删除同步**：通过轻量全量 ID 拉取检测云端已删除的笔记，按用户策略（通知/回收站/归档）处理本地副本
- 🌳 **父子笔记关系**：支持本地文件夹层级与云端 parent_id 的双向映射，保持笔记组织结构一致
- 🔀 **冲突合并 UI**：提供三栏对比界面（本地/远端/合并结果），用户可交互式解决同步冲突
- 🏷️ **标签差集推送**：仅推送变化的标签（新增/删除），减少 API 调用量
- ⚙️ **灵活触发模式**：支持手动触发、保存时自动同步、定时同步三种模式
- 🔐 **冲突策略配置**：支持本地优先、远端优先、标记冲突、交互式合并四种策略

### 修复 (Fixed)
- 🐛 **鉴权头规范**：修复 Authorization 头格式，按官方文档使用原始 API Key（可配置是否添加 Bearer 前缀）

---

## [3.4.0] - 2026-05-19

### 新增 (Added)
- 🔍 **OpenAPI 语义检索（RAG）**：集成 Get 笔记云端向量语义检索能力，支持全局召回和知识库内召回
- 📌 **侧边栏检索面板**：常驻侧边栏显示与当前笔记相关的云端内容，支持实时跟随模式
- ✏️ **划词搜索**：选中文本后快速触发语义检索，查找相关笔记、博主内容、直播片段
- 🎯 **智能结果展示**：区分本地已有笔记（直接打开）和云端独有内容（预览 Modal），支持一键同步到本地
- 📊 **知识库范围筛选**：可选择搜索全局或指定知识库，精准定位相关内容
- ⚡ **实时跟随模式**：编辑笔记时自动显示相关云端内容作为参考素材（可配置 debounce 时间）
- 🎨 **类型徽章**：结果列表显示笔记类型（个人笔记/博主内容/直播转写/得到电子书/外部网页）

---

## [3.3.0] - 2026-05-18

### 新增 (Added)
- 🚀 **OpenAPI 同步通道**：新增基于 Get 开放平台 REST API 的同步方式，与传统 Playwright + ZIP 导出通道并存
- 🔑 **OpenAPI 凭证管理**：支持配置 Client ID 和 API Key，提供连接测试功能
- 📚 **知识库同步**：支持同步自有知识库和订阅的外部知识库，按专题维度组织笔记
- 👥 **订阅博主同步**：抓取关注的创作者（抖音、微信、得到等）发布的内容到本地
- 🎙️ **直播转写同步**：同步已完成 AI 转写的直播内容（含 AI 摘要和完整转写文本）
- 🌳 **多级父子目录树**：还原云端笔记的嵌套关系为本地物理文件夹层级（可选）
- 🔄 **双通道共存**：用户可在 Playwright 模式和 OpenAPI 模式之间切换，两种通道共享目录结构和增量记录
- 📱 **移动端支持**：OpenAPI 通道全程使用 Obsidian 内置 API，完美支持移动端（Playwright 模式仍仅限桌面端）
- 🎯 **统一命名规范**：采用 Obsidian 社区最佳实践，文件名直接使用笔记标题，重名冲突使用 ` (2)` ` (3)` 后缀
- 📎 **按 noteId 分组附件**：OpenAPI 通道的附件按笔记 ID 组织到子目录，方便管理
- ⚙️ **增量同步策略**：支持全量和增量两种同步策略，增量模式通过 uid 和 modified 时间戳智能判断更新
- 🔒 **同步锁机制**：确保同一时刻只有一个同步任务运行，避免并发冲突
- 📊 **同步历史记录**：记录最近 20 次同步结果（成功/失败、耗时、新增/更新条数），方便排查问题
- 🚫 **取消机制**：支持在同步进行中随时中断，立即停止网络请求和文件写入

### 更改 (Changed)
- 📁 **目录结构优化**：
  - 个人笔记：`{getTarget}/{memoTarget}/{YYYY-MM-DD}/{标题}.md`
  - 知识库：`{getTarget}/知识库/{专题名}/{标题}.md`
  - 订阅知识库：`{getTarget}/订阅知识库/{专题名}/{标题}.md`
  - 订阅博主：`{getTarget}/订阅博主/{平台}_{账号名}/{标题}.md`
  - 直播课：`{getTarget}/直播课/{专题名}/{标题}.md`
- 🏷️ **标签规范化**：统一标签清理规则（空格转下划线、特殊符号转义、全数字加前缀），与 ZIP 通道保持一致
- 📄 **YAML 字段扩展**：frontmatter 新增 `note_type`（笔记类型）、`topic_id`（专题 ID）、`topic_name`（专题名称）等字段

### 修复 (Fixed)
- 🐛 **大文件处理**：桌面端优先使用原始路径，移动端自动使用流式临时文件，避免内存占用
- 🐛 **路径冲突检测**：通过 uid 精准判断是否为同一笔记，避免误覆盖不同笔记
- 🐛 **时间戳比较**：统一转换为毫秒时间戳后比较，避免字符串比较导致的判断错误

---

## [3.2.0] - 2026-05-17

### 修复 (Fixed)
- 📝 **文档合规性修复**: 移除所有外部图片链接，使用本地截图，符合 Obsidian 官方审核要求
- 🔗 **README 优化**: 添加英文描述，确保国际用户可以理解插件功能
- 🖼️ **图片资源**: 添加本地 images 文件夹，包含插件界面截图

---

## [3.1.0] - 2026-04-25

### 新增 (Added)
- 🚀 **附件可选导入**: 在 UI 界面新增了附件类型过滤器，允许用户自主选择导入哪些类型的附件（图片、音频、视频、文档），有效防止无用文件堆积。
- 🔗 **原文链接 YAML 自动化**: 自动从笔记正文中识别并提取“原文链接”，并将其清洗后存储为 YAML 正文属性中的 `source` 字段，实现更专业的元数据管理。
- 🛡️ **HTML 解析防御 (内容保真)**: 针对导出源文件中畸形或未闭合的 `<p>` 标签结构，实施了健壮的节点遍历机制，彻底杜绝了元数据清理时意外“吞噬”正文导致的内容丢失问题。

### 更改 (Changed)
- 📊 **大文件处理策略优化**: 重新设计了 ZIP 文件处理逻辑。桌面端优先使用原始路径以提升效率；针对移动端或受限环境（3GB+ 文件），引入了**真流式临时文件策略**，解决了 "The requested file could not be read" 的读取瓶颈。
- 🧹 **精细化转义清理**: 优化了 Markdown 后处理正则，仅脱敏影响阅读的 `\_` 和 `\.` 等杂音，完整保留方括号、星号、井号等对 Markdown 结构至关重要的转义。

### 修复 (Fixed)
- 🐛 **正文大面积丢失**: 修复了之前版本在处理特定 HTML 嵌套结构时，因元数据删除逻辑过于宽泛而导致正文大面积被误删的严重 Bug。
- 🐛 **代码块特殊字符还原**: 改进了 `<pre><code>` 内的 HTML 实体解码逻辑，确保代码块中的尖括号、连接符等特殊符号能被正确转回 Markdown。

---

## [3.0.0] - 2026-04-12


### 新增 (Added)
- 📄 **YAML 前方数据 (Properties)**: 将笔记元数据（标题、创建时间、标签）统一收纳进符合 Obsidian 官方标准的 YAML 属性区块中，不再是零散的正文文本。
- 📎 **通用附件兼容**: 针对原导出文件中的普通文件（如被 `<a>` 标签包裹的 PDF 等）新增完整的 Turndown 识别规则，使其能被无缝转存为 Obsidian 标准内部链接。

### 更改 (Changed)
- 📝 **标题表现形式 (ATX)**: 更新了 Markdown 渲染规则中的 `Heading` 选项，抛弃了容易串词的 `setext` (`======`) 风格，改为 Obsidian 最佳实践的标准井号 `atx` (`#`, `##`) 风格。

### 修复 (Fixed)
- 🐛 **移动端导入崩溃 (`undefined path`)**: 增加了通过原生 `FileReader` ArrayBuffer 向后兼容文件的提取逻辑，修复了在移动设备上因操作系统屏蔽底层路径导致选择 `.zip` 导入直接报错中止的问题。
- 🐛 **夹杂的有序/无序列表不换行**: 定位并修复了极高频出现的 得到大脑（原Get笔记） 嵌套列表（如 `<p>` 中内夹着 `* `、`- `、`1. ` 等）不被视作正确列表处理的排版问题。基于强化正则将其强制转换为标准列表。
- 🐛 **非法路径字符崩溃 (`ENOENT`)**: 修复了由于自动生成的笔记标题中含有被系统禁止的路径符号（如 `/`, `\`, `:`, `?` 等等）从而导致本地物理文件创建失败的黑洞 Bug，加入了安全字符清洗阶段。
- 🐛 **内嵌录音媒体丢失**: 修复了之前一处将 `div.attachment` 提前删去而跳过音频处理节点的代码，使诸如 `.mp3` 录音也能完美还原回原文中的 `![[ ]]` 内嵌播放器格式。
- 🐛 **图片附随狗皮膏药文件名**: 在提取 HTML 转 Markdown 的阶段前，通过代码摘除并移除了多余的 `.image-title` 这个 `span` 标签，还清纯粹的图文体验。
- 🐛 **Obsidian 空命名幽灵文件夹**: 摘除了配置路径最后的斜杠尾巴 `/get attachment/` -> `/get attachment` ，避开了导致 Obsidian API 在其下方误生成空子目录名的行为漏洞。
- 🐛 **多余不必要的反斜杠 (\)**: 在末期对 Turndown 的过度安全机制生成的各种反斜杠转义符号（如针对 `_` 星号 `*` 点号 `.` 形成的形如 `gk\_live`）进行一次全局替换和无害化清理，恢复绝佳的文本阅读体验。

---

## [2.0.0] - 2026-01-12

### 🎉 重大发布 - 全面品牌升级

这是一个大版本更新，插件从 "flomo" 全面品牌化更名为 "得到大脑（原Get笔记）"，并在用户界面上做出了重大改进。

### 新增 (Added)
- ✨ **最新 UI 设计**: 具有现代化且有序界面的全面插件重构设计
- ✨ **中文 UI 支持**: 插件面板界面中文化全覆盖
- ✨ **新图标**: 采用全新记事本风格 SVG 应用图标
- ✨ **同步状态视窗**: 实时显示您的同步状态、最后同步时间和相关笔记计数
- ✨ **清理同步历史开关**: 增加一键按钮清除一切同步残留数据并允许全局重置覆盖式导入
- ✨ **综合性全量文档**: 完全重写 README 开源主页介绍，并引入图文交互指南
- ✨ **开发者参与守则**: 为愿意打磨贡献代码的用户撰写了 CONTRIBUTING.md
- ✨ **未来产品路线图**: 增加 ROADMAP.md 展示中远期的功能研发预想表
- ✨ **底层架构文档**: 增加 ARCHITECTURE.md 诠释本项目设计与工程方法论
- ✨ **英文文档**: 提供面向全球用户的 README.en.md


### 更改 (Changed)
- 🔄 **全局重新命名**: 处理了包含 "flomo" 的一切命名空间为 "Get" 亦或 "得到大脑（原Get笔记）"：
  - 目录架构: `lib/flomo/` → `lib/get/`
  - 核心类: `FlomoImporter` → `GetImporter`, `FlomoCore` → `GetCore` 等等
  - 变量设置: `flomoTarget` → `getTarget`
- 🎨 **面板设置块优化**: 进行整理归类，模块更为清晰:
  - 手动导入 (Manual Import)
  - 基础设置 (Basic Settings)
  - 视觉效果选项 (Visualization Settings)
  - 高级选项 (Advanced Options)
  - 自动同步 (Auto Sync)
  - 数据管理 (Data Management)

### 修复 (Fixed)
- 🐛 **白板构建路径 Bug**: 修复了由于寻址异常导致画布最终为透明或空白的问题
- 🐛 **时间轴瞬间逆转**: 修正日期读取参数确保最新的信息流顶置在前
- 🐛 **HTML 树报错**: 通过兼容方案解决了被卡退出现的 `cloneNode is not a function` 依赖问题
- 🐛 **自动劫持登入**: 修正了登录的监控 URL 事件范围以保证静默提取

---

## [1.4.0] - 2025-11-03

### 新增 (Added)
- 🔇 **隐秘后台同步**: 导出和计算进程转入完全静默，不再弹窗滋扰
- 🔄 **智能变更捕捉**: 可感知旧内容修改并在导入过程重写本地被改动笔记

### 更改 (Changed)
- 📁 **精简附件树状结构**:
  - 旧: `get picture/file/2025-11-03/4852/filename.m4a`
  - 新: `get attachment/2025-11-03/filename.m4a`

### 修复 (Fixed)
- 🐛 **图像抓取失败**: 修复全局正则针对部分 `![text]()` 失灵的 Bug

---

## [1.3.0] - 2025-09-15

### 新增 (Added)
- 支持 Zip 包本体离线手动直录导入
- 增加设定自动小时级定时轮询功能
- 允许定义自动渲染的 Canvas 最大容忍尺寸

---

## [1.2.0] - 2025-07-20

### 新增 (Added)
- 双向链接互导功能支持（预览版）

---

## [1.1.0] - 2025-05-10

### 新增 (Added)
- 热词时刻追踪显示
- 插件启动阶段自动发车
